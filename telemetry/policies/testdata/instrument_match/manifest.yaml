schema_url: https://olly.garden/schemas/grafts/fixture/0.1.0
description: >
  Fixture proving `instrument_match` fires: an imported convention redeclared with the wrong instrument type.
stability: development
dependencies:
  - schema_url: https://opentelemetry.io/schemas/1.44.0
    registry_path: /semconv/
